<?xml version="1.0" encoding="utf-8"?>
<!-- Créez ce fichier dans res/xml/network_security_config.xml -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="false">
        <!-- Configuration spécifique pour votre serveur Wazuh -->
        <domain includeSubdomains="false">159.65.120.14</domain>
        <trust-anchors>
            <!-- Accepter les certificats système + auto-signés pour Wazuh -->
            <certificates src="system"/>
            <certificates src="user"/>
        </trust-anchors>
    </domain-config>

    <!-- Configuration par défaut sécurisée -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
</network-security-config>